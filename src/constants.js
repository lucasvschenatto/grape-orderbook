"use strict"

const THRESHOLD_TIME = 3000

const ORDER_SIDE = {
  BUY: "buy",
  SELL: "sell",
}
const ADD_ORDER_SERVICE = "add_order_service"

module.exports = { ORDER_SIDE, THRESHOLD_TIME, ADD_ORDER_SERVICE }
